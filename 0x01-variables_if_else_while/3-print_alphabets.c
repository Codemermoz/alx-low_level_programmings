#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0
 */
int main(void)
{
	char c = 'a';

	while (c <= 'z')
	{
		putchar(c);
		c++;
	}

	while (c <= 'z')
	{
		putchar(ch);
		c++;
	}

	putchar('\n');
	return (0);
}
